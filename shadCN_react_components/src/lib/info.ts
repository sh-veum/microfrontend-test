const Info = {
  framework: "React",
  name: "React application using shadCN components",
  port: `port: ${import.meta.env.VITE_PORT}`,
  fullURL: `${window.location.protocol}//${window.location.hostname}:${window.location.port}`,
};

export default Info;
